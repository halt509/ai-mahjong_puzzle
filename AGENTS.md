# AGENTS.md

このファイルは、このリポジトリで作業するコーディングエージェント向けの共通ルールです。

## 1. プロジェクト

Pyxelで、麻雀を題材にした8×8の牌上書きパズルゲームを制作します。

- ゲーム全体の仕様: `docs/PROJECT_SPEC.md`
- 開発段階と完了条件: `docs/ROADMAP.md`
- ADR: `docs/adr/`

## 2. 最優先ルール

- 原則として日本語で応答・報告してください。
- ユーザーから指定された現在フェーズだけを実装してください。
- 将来フェーズの機能を、便利そうだからという理由で先回りして実装しないでください。
- 最新のユーザー指示と文書が競合した場合は、最新のユーザー指示を優先してください。
- 未確定仕様を黙って確定しないでください。
- 実装継続に仮定が必要な場合は、後から変更しやすい形で置き、READMEまたは作業報告へ明記してください。
- 既存の仕様、テスト、公開APIを変更するときは、理由と影響範囲を説明してください。

## 3. 現在のスコープ

現在フェーズは `docs/ROADMAP.md` を確認してください。

フェーズ1では、Pyxelを使わず、純粋なPythonのルール判定エンジンと自動テストだけを実装します。

フェーズ1では次を実装しません。

- Pyxel画面
- 8×8盤面の描画
- 入力処理
- テトリミノ操作
- 川の画面表示
- 効果音・アニメーション
- セーブ・ハイスコア

## 4. 設計方針

- ルールロジックをUI、Pyxel、グローバル状態から分離してください。
- 牌、和了分解、役、カン、ドラ、得点、ゲーム状態の責務を分けてください。
- Python 3.11以降を想定し、型ヒントを付けてください。
- `dataclass`、`Enum`、不変データを適切に使ってください。
- 役と得点は設定として差し替えられるようにしてください。
- ルールがコードから読み取れることを優先してください。
- 将来の可能性だけを理由に過剰な抽象化をしないでください。
- 外部ライブラリは最小限にしてください。
- エラーを握りつぶさないでください。
- 不正な牌数、不正な牌データ、不可能な状態には明示的な例外を返してください。

## 5. 独自麻雀ルール上の注意

このゲームは通常の麻雀をそのまま再現するものではなく、8牌用の独自ルールです。

- 和了判定は横1行の8牌です。
- 基本形は「3枚組＋3枚組＋2枚組」です。
- 牌の左右順は問いません。
- 3枚組は順子または刻子です。
- 2枚組は対子です。
- 同一牌4枚を4枚組の面子として扱いません。
- カンは和了とは別のイベントとして扱います。
- 役が1つもなければ和了扱いにしません。
- ドラだけでは和了できません。
- 裏ドラ、赤ドラ、鳴き、符計算は初期版に含めません。

詳細は `docs/PROJECT_SPEC.md` を参照してください。

## 6. テスト

- テストは `pytest` を使用してください。
- 不具合修正時は、修正前に失敗を再現するテストを追加してください。
- 判定ロジックの変更には、正常系・境界値・不成立例を含めてください。
- 同じ入力が安定して同じ結果を返すようにしてください。
- 入力順の違いで判定が変わらないことを確認してください。
- 複数の分解候補を漏らさないでください。
- 同じ牌を複数の面子へ重複使用しないでください。
- 牌総数や個体IDの整合性を確認してください。
- 作業完了前に全テストを実行してください。

## 7. 文書

- READMEにセットアップ、テスト方法、主要モジュール、サンプル実行を記載してください。
- 仮定や保留事項をREADMEに残してください。
- 仕様変更がある場合は、関連する文書も同時に更新してください。
- 新しく作成・大きく更新した文書には、作成日または更新日と、使用したAIモデル名を記載してください。
- AIモデル名が実行環境から確認できない場合は、推測せず「不明」と記載してください。

## 8. ADR

構造、公開API、ルール解釈、依存関係など、後から理由を知る必要がある決定は `docs/adr/` にADRとして残してください。

ADRが必要な例:

- 牌の識別方法を決める
- 和了分解アルゴリズムを選ぶ
- 役判定APIを変更する
- カンと和了の処理順を決める
- 136枚の配分方式を確定する
- Pyxelとルールエンジンの境界を変更する

単純な誤字修正、テスト追加、仕様どおりの小規模実装にはADRは不要です。

ADRは `docs/adr/0000-template.md` を複製して作成してください。

## 9. Gitとコミット

Git操作を許可されている場合だけ実施してください。

- 1コミットを1つの意味のまとまりにしてください。
- リファクタリングと機能変更を可能な限り分けてください。
- テストが失敗した状態を意図せずコミットしないでください。
- コミットメッセージは日本語でも英語でも構いませんが、内容が分かるようにしてください。
- 設計判断を含むコミットでは、対応するADRも同じコミットへ含めてください。
- ユーザーから明示的に依頼されるまで、pushやPR作成を行わないでください。

## 10. 作業の進め方

1. 関連する仕様と現在フェーズを読みます。
2. 既存コードとテストを確認します。
3. 変更範囲を小さく定義します。
4. 必要なテストを先に用意します。
5. 実装します。
6. 全テストを実行します。
7. README、仕様、ADRの更新要否を確認します。
8. ユーザー向けの確認手順をまとめます。

質問が必要なのは、回答によって実装構造やゲーム挙動が大きく変わる場合だけです。軽微な保留事項は、仮定として明示した上で作業を進めてください。

## 11. 完了報告

各作業の最後に、次を簡潔に報告してください。

- 実装内容
- 変更ファイル
- テスト結果
- 採用した仮定
- 未解決事項
- ユーザーが確認する項目
- ADRの有無
- コミットの有無

## 12. 禁止事項

- 仕様にないゲーム機能を勝手に追加しないでください。
- 将来フェーズのコードを先回りして大量に作らないでください。
- 役や得点をコードの複数箇所へ直書きしないでください。
- Pyxel依存をルールエンジンへ持ち込まないでください。
- テストを削除して失敗を隠さないでください。
- 例外を広範な `except Exception` で握りつぶさないでください。
- 外部通信や不要な依存パッケージを追加しないでください。
- ユーザーの許可なく秘密情報、生成物、開発用指示ファイルを公開環境へ配置しないでください。
